name: loading_ui_flutter_example
description: "Interactive showcase of all loading_ui_flutter widgets."
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.5
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  loading_ui:
    path: ../
  url_launcher: ^6.3.1
  flutter_svg: ^2.0.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
